# Phase 4 Summary — Reporting & Exports

## Delivered

- Dodano komendę `export` z parametrami:
  - `--id <scanId>`
  - `--format json|csv|json,csv`
  - `--output <path>`
- JSON i CSV obejmują spójne dane raportowe (statusy, reason, vendor, decompile, retry).
- Eksport tworzy artefakty raportowe w zadanej lokalizacji.
