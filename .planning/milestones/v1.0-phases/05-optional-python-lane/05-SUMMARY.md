# Phase 5 Summary — Optional Python Lane

## Delivered

- `scan` domyślnie skanuje tylko `.dll/.exe`.
- Dodano przełącznik `--include-python` dla `.py/.whl`.
- Zachowano model pasywnej analizy (brak uruchamiania kodu Python).
