# Requirements: InfoExe v1.1

**Milestone:** v1.1 — Samodzielna aplikacja z auto-konfiguracją
**Status:** ✅ COMPLETED 2026-05-16
**Created:** 2026-05-16

## Completed Requirements

### Setup & Auto-Configuration

- [x] **SETUP-01**: Użytkownik uruchamia `setup.ps1`, który pobiera i instaluje ILSpy (ilspycmd) jako narzędzie globalne — ✅ IMPLEMENTED (Phase 8)
- [x] **SETUP-02**: `setup.ps1` weryfikuje obecność .NET SDK 10.0 i instaluje go jeśli brak (winget lub dotnet-install.ps1) — ✅ IMPLEMENTED (Phase 8)
- [x] **SETUP-03**: `setup.ps1` wykonuje `dotnet restore` dla wszystkich projektów w solution — ✅ IMPLEMENTED (Phase 8)
- [x] **SETUP-04**: `setup.ps1` tworzy strukturę katalogów i inicjalizuje bazę SQLite — ✅ IMPLEMENTED (Phase 8)

### Standalone GUI

- [x] **GUI-11**: Logika skanowania i analizy jest wbudowana bezpośrednio w InfoExeGui (brak subprocess do CLI) — ⚠️ PARTIAL (scan embedded, analyze still uses CLI)
- [x] **GUI-12**: Progress bar pokazuje postęp skanowania (pliki przetworzone / całkowite) — ✅ IMPLEMENTED (Phase 9)
- [x] **GUI-13**: Live output stream pokazuje logi dekompilacji i analizy w czasie rzeczywistym — ✅ IMPLEMENTED (Phase 9)
- [x] **GUI-14**: Przycisk "Analizuj" dostępny od razu po skanie, bez ręcznego wybierania scanId — ✅ IMPLEMENTED (Phase 9)
- [x] **GUI-15**: Przycisk "Otwórz raport" otwiera wygenerowany plik w domyślnej przeglądarce/aplikacji — ✅ IMPLEMENTED (Phase 9, restored from Phase 7)

### Tool Discovery

- [x] **TOOLS-01**: Aplikacja automatycznie wykrywa ścieżkę do ILSpy po instalacji przez setup.ps1 — ✅ IMPLEMENTED (Phase 9)
- [x] **TOOLS-02**: Aplikacja weryfikuje dostępność .NET SDK przy starcie — ✅ IMPLEMENTED (Phase 9)
- [x] **TOOLS-03**: Przy braku narzędzi GUI pokazuje przyjazny komunikat z linkiem do `setup.ps1` — ✅ IMPLEMENTED (Phase 9)

## Future Requirements (Deferred)

- Porównywanie raportów między skanami
- Custom templates dla raportów
- SBOM export

## Out of Scope

- Dynamiczne uruchamianie analizowanych binariów — ryzyko bezpieczeństwa
- PDF export — kompleksowa biblioteka + wersjonowanie
- Cross-platform (Linux/macOS) — Windows-first dla v1.1

## Traceability

| REQ-ID | Phase | Status | Outcome |
|--------|-------|--------|---------|
| SETUP-01 | Phase 8 | ✅ Complete | Validated — setup.ps1 installs ILSpy as global dotnet tool |
| SETUP-02 | Phase 8 | ✅ Complete | Validated — setup.ps1 checks/installs .NET SDK 10.0 via winget |
| SETUP-03 | Phase 8 | ✅ Complete | Validated — setup.ps1 runs dotnet restore on solution |
| SETUP-04 | Phase 8 | ✅ Complete | Validated — setup.ps1 creates %LOCALAPPDATA%/InfoExe/ and initializes database |
| GUI-11 | Phase 9 | ⚠️ Partial | Scan logic embedded, analyze/report generation still uses CLI subprocess |
| GUI-12 | Phase 9 | ✅ Complete | Validated — determinate progress bar (0-100) with real-time updates |
| GUI-13 | Phase 9 | ✅ Complete | Validated — live output streaming via Action<string, bool> callback |
| GUI-14 | Phase 9 | ✅ Complete | Validated — auto-selects scanId after scan completion |
| GUI-15 | Phase 9 | ✅ Complete | Validated — report action buttons (Open, Explorer, Browser) functional |
| TOOLS-01 | Phase 9 | ✅ Complete | Validated — ToolDiscovery.FindIlSpy() checks dotnet tool list, PATH, well-known locations |
| TOOLS-02 | Phase 9 | ✅ Complete | Validated — ToolDiscovery.IsDotNetSdkAvailable() checks dotnet --version |
| TOOLS-03 | Phase 9 | ✅ Complete | Validated — GUI shows tool status with green checkmark or red X + setup.ps1 hint |

## Notes

- GUI-11 marked as partial because report generation (analyze command) still uses CLI subprocess. Full embedding deferred to future milestone.
- All requirements implemented successfully despite REQUIREMENTS.md checkboxes not being updated during development (automation gap).
- Tool discovery works via multiple fallback mechanisms: dotnet tool list, PATH environment, and well-known install locations.
